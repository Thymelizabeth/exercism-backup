name: allergies
version: 1.2.0.7

dependencies:
  - base

library:
  exposed-modules: Allergies
  source-dirs: src
  ghc-options: -Wall
  dependencies:
    - containers
  # - foo       # List here the packages you
  # - bar       # want to use in your solution.

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - allergies
      - containers
      - hspec
      - QuickCheck
